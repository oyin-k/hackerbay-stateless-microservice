const express = require('express');
const router = express.Router();
const thumbnailCreationController = require('../controller/thumbnailCreationController');

// router.post('/image/createThumbnail', thumbnailCreationController);

module.exports = router;
